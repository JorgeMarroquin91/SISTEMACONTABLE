@model IEnumerable<SistemaContable.Models.COTOS_DE_MATERIAL>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FECHA_DE_INGRESO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.COSTO_DE_COMPRA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.COSTO_UNITARIO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FECHA_DE_INGRESO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.COSTO_DE_COMPRA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.COSTO_UNITARIO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_COSMATERIAL }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_COSMATERIAL }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_COSMATERIAL })
        </td>
    </tr>
}

</table>
