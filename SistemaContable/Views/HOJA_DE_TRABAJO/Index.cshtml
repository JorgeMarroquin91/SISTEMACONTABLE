@model IEnumerable<SistemaContable.Models.HOJA_DE_TRABAJO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SALARIO_HOR_NOMAL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SALRIOHORASEXTRAS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HORASTRABAJADAS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HORASEXTRAS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SALARIO_HOR_NOMAL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SALRIOHORASEXTRAS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HORASTRABAJADAS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HORASEXTRAS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_HOJA }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_HOJA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_HOJA })
        </td>
    </tr>
}

</table>
