@model IEnumerable<SistemaContable.Models.PERIODO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DEBE_)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FIN_PERIODO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DEBE_)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FIN_PERIODO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NUMPERIODO }) |
            @Html.ActionLink("Details", "Details", new { id=item.NUMPERIODO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NUMPERIODO })
        </td>
    </tr>
}

</table>
