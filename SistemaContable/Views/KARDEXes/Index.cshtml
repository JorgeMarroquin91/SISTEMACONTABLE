@model IEnumerable<SistemaContable.Models.KARDEX>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.COSTOUNITARIO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FECHA_INGRESO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FECHA_DE_SALIDA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.COSTOUNITARIO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FECHA_INGRESO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FECHA_DE_SALIDA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NUMKARDEX }) |
            @Html.ActionLink("Details", "Details", new { id=item.NUMKARDEX }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NUMKARDEX })
        </td>
    </tr>
}

</table>
