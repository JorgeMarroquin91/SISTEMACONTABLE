@model IEnumerable<SistemaContable.Models.CUENTA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOM_CUENTA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOM_CUENTA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.COD_CUENTA }) |
            @Html.ActionLink("Details", "Details", new { id=item.COD_CUENTA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.COD_CUENTA })
        </td>
    </tr>
}

</table>
